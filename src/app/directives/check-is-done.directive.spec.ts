import { CheckIsDoneDirective } from './check-is-done.directive';

describe('CheckIsDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckIsDoneDirective();
    expect(directive).toBeTruthy();
  });
});
